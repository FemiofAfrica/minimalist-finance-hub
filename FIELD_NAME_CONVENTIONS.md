# Field Name Conventions

This document outlines the naming conventions used throughout the Kpege application to help maintain consistency and avoid confusion.

## Overview
This document outlines the naming conventions used throughout the Kpege application to help maintain consistency and avoid confusion.

## General Principles

## Database Schema vs UI State Naming

Our application deals with two different naming patterns:

1. **Database Schema**: Uses `name` and `type` columns in the `categories` table
2. **UI and API**: Uses `category_name` and `