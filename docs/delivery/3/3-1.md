# [3-1] Create category aggregation service

[Back to task list](../tasks.md)

## Description
Build a backend/utility service that aggregates transaction data by category and time period, returning totals and percentages needed for all category-level visualisations.

## Status History
| Timestamp | Event Type | From Status | To Status | Details | User |
|-----------|------------|-------------|-----------|---------|------|
| {{date:YYYY-MM-DD HH:mm:ss}} | Created | N/A | Proposed | Task file created | ai-agent |

## Requirements
1. Function
```ts
getCategoryTotals(userId: string, months: number): Promise<CategoryAggregate[]>
```
   returns each category's sum, percentage of total, and currency.
2. Support filtering by category type (expense/income/transfer).
3. Leverage existing `dashboardService` DB access pattern.
4. Use named constants for magic numbers.

## Implementation Plan
- Define `CategoryAggregate` interface in `src/types/analytics.ts`.
- Implement service in `src/services/categoryAnalyticsService.ts`.
- Add unit tests with mocked Supabase client.

## Verification
- Unit tests for correct SQL grouping and percentage maths.
- Integration test using seed data fixture.

## Files Modified
- src/services/categoryAnalyticsService.ts (new)
- src/types/analytics.ts (new)
- test/unit/categoryAnalyticsService.test.ts (new)

## Test Plan
See Verification section. 