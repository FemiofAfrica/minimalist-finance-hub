# [3-2] Implement category change analysis algorithms

[Back to task list](../tasks.md)

## Description
Compute month-over-month (MoM) and year-over-year (YoY) change metrics per category, identifying significant (>25%) shifts and producing data structures for "biggest movers" widgets.

## Status History
| Timestamp | Event Type | From Status | To Status | Details | User |
|-----------|------------|-------------|-----------|---------|------|
| {{date:YYYY-MM-DD HH:mm:ss}} | Created | N/A | Proposed | Task file created | ai-agent |

## Requirements
1. Extend `categoryAnalyticsService` with
```ts
getCategoryChanges(userId: string, months: number): Promise<CategoryChange[]>;
```
2. Detect both absolute and percentage change.
3. Flag `isSignificant` when > SIGNIFICANT_CHANGE_THRESHOLD (25%).
4. Constant thresholds defined in one place.

## Implementation Plan
- Add SQL window functions or two-query comparison for previous periods.
- Write helper util for percentage math (reuse existing utils if any).
- Unit tests cover edge cases (division by zero, new category appearance).

## Verification
- Unit tests pass.
- Integration test validates outputs against fixture.

## Files Modified
- src/services/categoryAnalyticsService.ts
- test/unit/categoryChangeAnalysis.test.ts

## Test Plan
Unit + integration coverage. 