# [3-9] Fix currency & category-type issues in Smart Insights

[Back to task list](../tasks.md)

## Description
Address two defects reported in Smart Insights:
1. "Salary" (and all income categories) should never be classified as spending. Insights such as spending spikes/drops must exclude income categories.
2. Aggregated NGN amounts are inflated (e.g., NGN 175,350,420). Root cause is likely that raw transaction amounts are stored in kobo (×100). We need to normalise amounts before insight generation.

## Status History
| Timestamp | Event Type | From Status | To Status | Details | User |
|-----------|------------|-------------|-----------|---------|------|
| 2025-06-17 11:10:00 | Created | N/A | Agreed | User approved task creation | ai-agent |
| 2025-06-17 11:10:00 | Status Change | Agreed | InProgress | Started implementation | ai-agent |
| 2025-06-17 14:38:00 | Status Change | InProgress | Done | Implementation completed and tested | ai-agent |

## Requirements
1. Income categories (type === "income") must be excluded from spending-based insight types: `spending_spike`, `spending_drop`, `new_category`, `missing_category`.
2. Normalise raw transaction amounts:
   - If currency is NGN and absolute amount ≥ 10,000,000 treat value as kobo and divide by 100.
   - Apply the same logic for getCategoryTotals and getCategoryTotalsPeriod.
3. Update unit tests and integration tests to cover these cases.
4. Ensure no regression in existing tests.

## Implementation Plan
- Add `normalizeAmount` util inside `categoryAnalyticsService.ts`.
- Update amount processing in both aggregation functions.
- Update `generateChangeBasedInsights` to `continue` early for income categories.
- Extend existing unit tests or add new ones.
- Run full test suite and adjust snapshots if necessary.

## Verification
- ✅ Unit tests for normalization logic pass (23/23 tests passing)
- ✅ CategoryInsights unit tests confirm income categories excluded from spending insights
- ✅ Currency normalization working: NGN amounts ≥10M treated as kobo and divided by 100
- ✅ All spending-based insights (spending_spike, spending_drop, new_category, missing_category) exclude income categories
- ✅ Console logging shows "Skipping income category [Name] from spending insights" for income types
- ✅ Manual verification shows realistic NGN values and no income category spending alerts

## Files Modified
- `src/services/categoryAnalyticsService.ts`
- `src/services/insights/categoryInsights.ts`
- `test/unit/categoryAnalyticsService.test.ts`
- `test/unit/categoryInsights.test.ts`

## Test Plan
1. `npm run test:run` all tests pass.
2. Add a unit test with NGN value 10,000,000 (kobo) returns 100,000 after aggregation.
3. Add a test asserting income category excluded from spending_spike insights. 

## Implementation Details

### Currency Normalization
- Added `normalizeAmount()` function with NGN kobo threshold of 10,000,000
- Applied normalization in both `categoryAnalyticsService.ts` and `categoryInsights.ts`
- Amounts ≥ NGN 10M are divided by 100 (kobo to naira conversion)
- Amounts below threshold remain unchanged

### Income Category Exclusion
- Added income type filtering in `generateChangeBasedInsights()` function
- Added income type filtering in `generateOptimizationInsights()` function  
- Added income type filtering in `generatePatternBasedInsights()` function
- Income categories (type === 'income') are completely excluded from spending-based insights

### Test Coverage
- Added 4 comprehensive test cases covering both fixes
- Tests verify currency normalization works correctly for large and small amounts
- Tests verify income categories are excluded from all spending-based insight types
- All tests passing with verification of expected behavior 