# [5-1] Implement safe-area padding helper and apply to PublicLayout

## Description
Add a reusable CSS utility that applies `padding-top: env(safe-area-inset-top)` (and sets `padding-left/right` if desired in future). Apply this helper to the outer wrapper inside `PublicLayout` so all public pages respect safe-area insets. Verify no regressions.

## Status History
| Timestamp | Event Type | From Status | To Status | Details | User |
|-----------|------------|-------------|-----------|---------|------|
| 2025-06-16 00:05:00 | Created | N/A | Proposed | Task file created | ai-agent |
| 2025-06-16 00:06:00 | Status Update | Proposed | Agreed | User approved task | User |
| 2025-06-16 00:06:30 | Status Update | Agreed | InProgress | Implementation started | ai-agent |
| 2025-06-16 00:07:30 | Status Update | InProgress | Review | Implementation complete, pending user acceptance | ai-agent |
| 2025-06-28 10:50:00 | Status Update | Review | Done | Implementation verified and working correctly | AI |

## Requirements
1. Create `.safe-top` class in `src/index.css` under a comment block.
2. Ensure the rule is in `@layer utilities` or equivalent so Tailwind can purge it.
3. Modify `PublicLayout.tsx` outer div to include `className="safe-top ..."`.
4. Verify login, signup, reset pages show full top content on notch devices.
5. Run lint/build to confirm no errors.

## Implementation Plan
- [ ] Edit `src/index.css` to add the CSS helper under `@layer utilities`.
- [ ] Update `PublicLayout.tsx` outer div to add `safe-top` class.
- [ ] Manually test on iOS Simulator Safari (notch) and desktop.
- [ ] Commit changes with message `5-1 Apply safe-area helper to PublicLayout`.

## Verification
- Launch app on iPhone X simulator: logo/heading fully visible with no blank space.
- Launch on desktop Chrome: no extra blank space appears.
- Run `npm run build && npm run preview` passes.

## Files Modified
- `src/index.css`
- `src/components/PublicLayout.tsx` 