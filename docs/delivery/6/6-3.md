# [6-3] Refactor DashboardLayout & Sidebar spacing

## Description
Remove ad-hoc padding logic that manually references `BANNER_HEIGHT` and instead consume the new global spacing helper delivered in 6-2.  All dashboard-level views must automatically respect safe-area + banner height via the `.top-offset` utility and/or `useTopOffset()` hook.

## Status History
| Timestamp | Event Type | From Status | To Status | Details | User |
|-----------|------------|-------------|-----------|---------|------|
| {{date:YYYY-MM-DD HH:mm:ss}} | Created | N/A | Agreed | Task documented and approved | ai-agent |
| {{date:YYYY-MM-DD HH:mm:ss}} | Status Change | Agreed | InProgress | Started implementation | ai-agent |
| {{date:YYYY-MM-DD HH:mm:ss}} | Status Change | InProgress | Review | Implementation complete; awaiting review | ai-agent |
| {{date:YYYY-MM-DD HH:mm:ss}} | Status Change | Review | Done | User approved implementation | ai-agent |

## Requirements
1. Replace inline `paddingTop` calculations in `DashboardLayout.tsx` with `.top-offset` utility class (CSS handles safe-area & banner).
2. Remove banner visibility state from `DashboardLayout` – no longer needed.
3. Ensure outer wrapper still marks safe-area via `.safe-top` when banner is hidden (auth pages handled separately in PublicLayout).
4. Update `PublicLayout.tsx` similarly: use `.top-offset` when banner included; fall back to `.safe-top` only on auth pages.
5. Remove unnecessary imports of `BANNER_HEIGHT` in the affected components.
6. Update unit/integration tests that relied on explicit padding style if any (accept new approach).

## Implementation Plan
- [x] Update `DashboardLayout.tsx`.
- [x] Update `PublicLayout.tsx`.
- [x] Remove unused state & listeners.
- [x] Search for any remaining `BANNER_HEIGHT` references in layout context; replace if needed.
- [x] Run linter & tests; adjust failing snapshots.

## Verification
- Manual: toggle banner visibility and observe dashboard & public pages adjust instantly with smooth transition.
- Unit/Integration: revise tests to query for `.top-offset` class rather than inline padding.

## Files Modified
- `src/components/dashboard/DashboardLayout.tsx`
- `src/components/PublicLayout.tsx`
- (potentially) `src/components/DashboardSidebar.tsx` if spacing logic found.

## Test Plan (simple)
Rely on existing page/layout integration tests; update snapshots if needed. 