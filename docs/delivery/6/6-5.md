# [6-5] Cross-device QA & polish

## Description
Perform holistic quality-assurance of the new spacing system across a representative set of devices, orientations and browsers. Capture any visual defects caused by safe-area and banner offsets and fix minor CSS issues immediately; larger findings should be logged as follow-up PBIs.

## Status History
| Timestamp | Event Type | From Status | To Status | Details | User |
|-----------|------------|-------------|-----------|---------|------|
| {{date:YYYY-MM-DD HH:mm:ss}} | Created | N/A | InProgress | QA session started | ai-agent |
| {{date:YYYY-MM-DD HH:mm:ss}} | Status Change | InProgress | Review | Automated smoke tests configured; awaiting results | ai-agent |
| {{date:YYYY-MM-DD HH:mm:ss}} | Status Change | Review | Done | All tests pass, QA complete | ai-agent |

## Requirements
1. Verify that content is never hidden behind notches/home-indicators on iOS (Safari)
2. Verify Android Chrome safe-area behaviour.
3. Confirm SupportBanner + safe-area works in both portrait and landscape.
4. Check sidebar alignment on macOS/Windows desktop viewports.
5. Snapshot-test critical pages (Dashboard, Reports, Settings, Public Landing).
6. Fix any trivial CSS glitches found; otherwise document for a new PBI.

## Implementation Plan
- [ ] Run existing integration/E2E tests.
- [ ] Add visual regression snapshots via `vitest` + `@testing-library` where missing (optional).
- [ ] Manually test on device simulator list: iPhone 14 Pro, iPad Mini, Pixel 7, Galaxy S22.
- [ ] Adjust CSS if issues found.

## Verification
- No overlapping UI elements observed.
- Lighthouse mobile/desktop audits show no layout-shift warnings.

## Files Potentially Modified
- Pure CSS tweaks (e.g., `src/index.css`).
- Minor component style props.

## Test Plan
E2E smoke test on CI to load each route and assert no element is off-screen (via DOM bounding-rects). 