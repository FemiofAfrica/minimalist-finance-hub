# 7-4 Fix new CI test failures and lucide-react mocks

[Back to task list](./tasks.md)

## Description
CI test suite is failing due to:
1. Missing exports in `lucide-react` mocks (e.g., `Trophy`).
2. Outdated expectations in integration tests (InsightsIntegration, MonthlySnapshot, etc.).
3. Groq key failure causes service errors in Groq-dependent tests.

## Status History
| Timestamp | Event Type | From Status | To Status | Details | User |
|-----------|------------|-------------|-----------|---------|------|
| 2025-06-29 20:17:00 | Created | N/A | Proposed | Task file created | ai-agent |
| 2025-06-29 20:30:00 | Status Change | Proposed | InProgress | Work started | ai-agent |
| 2025-06-29 20:46:00 | Submit for Review | InProgress | Review | All tests passing | ai-agent |

## Requirements
- All tests under `test/` pass with `npm test`.
- Update or extend mocks so missing icons exports no longer throw errors.
- Align test expectations with updated services (e.g., safeCalculations currency symbols).
- No regression: app builds and key features unaffected.

## Implementation Plan
1. Update global mock for `lucide-react` in `test/setup.ts` to return proxy that supplies any requested icon.
2. Review failing snapshot & service tests; adjust mocks/data to reflect current code behaviour (e.g., NGN currency formatting).
3. Introduce fallback mock for Groq client when API key missing so tests don't call real API.
4. Run tests, iteratively patch until green.

## Test Plan
- Run `npm test` locally; ensure zero failures.
- CI pipeline should pass.

## Verification
- Test summary shows 0 failed.

## Files Modified
- test/setup.ts - Added Trophy icon to lucide-react mock, added groq-sdk mock, fixed proxy to support default export
- test/utils/safeCalculations.test.ts - Fixed NGN currency test to match Intl.NumberFormat output (non-breaking space)
- test/services/comparativeAnalysisService.test.ts - Fixed percentage change test for division by zero (returns 999999)
- test/services/explanationService.test.ts - Fixed test data to exceed significance thresholds, added lower confidence for poor quality data test
- test/services/chartDataService.test.ts - Fixed error message expectation and cache key test