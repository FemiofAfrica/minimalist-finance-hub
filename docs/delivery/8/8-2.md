# 8-2 Update Footer Links

[Back to task list](./tasks.md)

## Description
Add navigation links for “Privacy” and “Blog” to the shared footer component across the application.

## Status History
| Timestamp | Event Type | From Status | To Status | Details | User |
|-----------|------------|-------------|-----------|---------|------|
| 2025-06-29 10:06:00 | Created | N/A | Proposed | Task file created | ai-agent |
| 2025-06-29 10:26:00 | Status Change | Proposed | InProgress | Work started | ai-agent |

## Requirements
- Footer shows two links: “Privacy” → `/privacy` and “Blog” → `/blog`.
- Links should use existing link styling and open within the app router.
- No layout shift introduced.

## Implementation Plan
1. Locate shared `Footer` component (or create if not present) under `src/components/`.
2. Import `Link` from React Router (or respective routing lib).
3. Add `<Link>` elements for routes `/privacy` and `/blog`.
4. Ensure responsive layout does not break.
5. Update existing tests or add new unit test verifying links render and point to correct paths.

## Test Plan
- Render footer in unit test; assert presence of two links with correct `href`s.
- Manual test: click links and ensure navigation.

## Verification
- Navigate to any page, verify footer now contains links and they navigate correctly.

## Files Modified
- src/components/Footer.tsx (or similar)
- Associated test files 